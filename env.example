# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Firebase Configuration
# Get your service account JSON from Firebase Console > Project Settings > Service Accounts
# Copy the entire JSON object and paste it here as a single-line string
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Authentication
JWT_SECRET=your_jwt_secret_key

# Google Perspective API (for toxicity detection)
GOOGLE_PERSPECTIVE_API_KEY=your_google_perspective_api_key

# Optional: Cron Secret (for cleanup endpoint)
CRON_SECRET=your_cron_secret

